import { test, expect } from '@playwright/test';

test('should render home feed', async ({ page }) => {
  // Task 5
  // First you'll need to login before you can test the home feed.
  // You can copy/paste the login logic from the login.spec.js
  // Then make some assertions about the home feed, without interacting with it.
});
